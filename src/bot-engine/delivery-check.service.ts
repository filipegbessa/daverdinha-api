import { Injectable } from '@nestjs/common';

@Injectable()
export class DeliveryCheckService {
  async start(conversation: unknown): Promise<void> {
    // Full implementation added in a later task.
  }
}
